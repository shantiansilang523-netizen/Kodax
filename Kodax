-- KillAllEnemies.lua

local KillAll = {}

-- enemies は「敵オブジェクトの配列」を想定
-- 各敵は health を持つテーブル
function KillAll.Run(enemies)
    for _, enemy in ipairs(enemies) do
        if enemy.health ~= nil then
            enemy.health = 0
        end
    end
end

return KillAll
